#include "sort.h"

/**
 * radix_sort - sort an array using radix sort
 * @array: array to sort
 * @size: size of array
 */
void radix_sort(int *array, size_t size)
{
	if (!array || !size)
		return;
}
