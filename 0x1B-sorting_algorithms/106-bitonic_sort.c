#include "sort.h"

/*
 * bitonic_sort - sort an array using bitonic sort
 * @array: array to sort
 * @size: size of array
 */
void bitonic_sort(int *array, size_t size)
{
	if (!array || !size)
		return;
}
