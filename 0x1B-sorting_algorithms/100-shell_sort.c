#include "sort.h"

/**
 * shell_sort - sort an array using Shell sort
 * @array: array to sort
 * @size: size of array
 */
void shell_sort(int *array, size_t size)
{
	if (!array || !size)
		return;
}
